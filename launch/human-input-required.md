# Human input required

These items block external publication or require a final human decision. Source drafts use the variable names shown here; public repository pages must not expose unresolved variables as clickable links.

| Input or decision | Variable | Why it is required | Acceptance evidence |
| --- | --- | --- | --- |
| Final update to the verified existing Figma Community listing | — | The canonical URL exists, but its public page still serves the prior version. | Review the prepared in-place update preview and explicitly authorize the final `Publish` action shown by the current UI. |
| Public GitHub repository URL | `{{GITHUB_REPOSITORY_URL}}` | External posts need a stable source CTA. | Confirm repository visibility and the intended canonical URL. |
| Public support destination | — | Community and launch pages need an actionable feedback route. | Reconfirm `https://github.com/Tmmy-inter/figma-product-design-director/issues` immediately before final publication. |
| Public case-study URL | `{{CASE_STUDY_URL}}` | External posts need a stable URL even though the local study is complete. | Publish or link the reviewed local case study without changing its limitations. |
| Demo video | `{{DEMO_VIDEO_URL}}` | Optional for launch channels; no video currently exists. | Review the final cut, captions, permissions, links, and factual claims. |
| GitHub Release URL | `{{RELEASE_URL}}` | Release copy cannot link to an unpublished Release. | Decide version, create the authorized tag/Release later, and verify the public URL. |
| Community publisher identity | — | Required for attribution and account authorization. | Confirm the exact public display name and that the user authorizes publishing from that account. |
| Controlled comparison interpretation | — | One paired run is non-deterministic and mixed, not causal or statistical validation. | Human reviewer approves the criterion table and confirms no stronger public claim is used. |
| Final Community screenshots | — | Seven reviewed PNGs are prepared in the unpublished update draft but are not public. | Reconfirm all 1920 × 1080 exports in the final Community preview and approve order, crop, text, and rights. |
| Runtime recheck before publication | — | The repository version was verified on 2026-09-01, but product behavior can change. | Confirm command visibility, existing-listing identity, support destination, and final preview in the publishing session. |
| Final product name and affiliation statement | — | The prior name began with “Figma”; the proposed display name is Product Design Director. | Approve the display name, technical skill name, and independent-project disclaimer. |
| Release version | — | Existing `v1.0.0` points to the pre-conversion commit. | Choose whether a future docs/distribution release should be `v1.0.1` or another version; do not move `v1.0.0`. |
| GitHub metadata changes | — | Current description/topics were inspected read-only; homepage is empty. | Approve exact description, homepage, topics, and social-preview upload before running any command. |
| External post copy and channel rules | — | Drafts must be adapted to current platform/community rules. | Review each final post in the target channel and authorize the specific account/action. |

No external publication, repository-setting change, Release creation, tag operation, or account use is authorized by this file.
